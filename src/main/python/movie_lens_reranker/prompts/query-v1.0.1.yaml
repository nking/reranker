# prompts/query-v1.0.0.yaml
name: "reranker query prompt"
version: "1.0.0"
author: "Nichole"
created_at: "2025-12-17"

model_config:
  model: "castorini/LiT5-Distill-base-v2"
  max_tokens_per_concat_block: 512
  max_passages: 100

template: |
  Based on the past data for user u_{{user_id}} and their age {{age}} rank the following movies for them:

# Optional: Reference the 'Golden Dataset' version used to test this prompt
eval_dataset_id: ""